Databases required 		() = modified by

User (node.js)
	id, 
	username, 
	password, 
	age, 
	height, 
	gender, 
	accessToken,
	refreshToken.

Exercise (flask)
User_id, date_initialized, total_time_taken, rep_count.

WorkoutModel
	userId
	workoutType
	startTime
	endTime


Types of Exercises (node)
exercise_name, description, image, models_used, model_url.

